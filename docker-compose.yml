version: '3.8'

services:
  react:
    build: .
    restart: always
    container_name: e-learning-react
    ports:
      - "3001:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
